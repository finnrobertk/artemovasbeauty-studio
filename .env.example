# Sanity Studio environment variables
# IMPORTANT: Variables prefixed with SANITY_STUDIO_ are exposed to the browser.
# Do NOT place secrets/tokens here. Keep secrets only on the server.

# Your Sanity project ID (see manage.sanity.io or sanity.json)
SANITY_STUDIO_PROJECT_ID=

# Dataset to use (e.g. production, staging, dev)
SANITY_STUDIO_DATASET=production
